[project]
name = "mcp-use-cases"
version = "0.1.0"
description = "Practical MCP and AI agent use cases"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]",
  "httpx",
  "pydantic>=2",
]

[tool.uv]
dev-dependencies = []
